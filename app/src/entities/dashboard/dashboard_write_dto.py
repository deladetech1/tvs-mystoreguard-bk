# Dashboard write DTOs
# Note: Dashboard is read-only, so no write DTOs are needed
# This file exists for consistency with other entities

from src.entities.dashboard.dashboard_base import DashboardBase

